version: 2
models:
  - name: fct_customer_revenue
    columns:
      - name: order_id
        tests: [unique, not_null]
      - name: customer_id
        tests: [not_null]
      - name: amount_usd
        tests:
          - dbt_utils.accepted_range:
              min_value: 0
